# FG830-ATX-v1 Protocol

## 1. 時系列

- **金曜09:00 JST — RECEIVE**  
  OS乱数とローカル計時ジッターを混合した262,144バイトを保存する。秘密鍵が指定する位置から128ビットを復号し、生データの圧縮ファイル、SHA-256、復号フレーム、各ビットのスコアを同じGitコミットへ固定する。

- **金曜20:00 JST — CHECK / TRANSMIT**  
  みずほ銀行のロト7当せん番号案内をブラウザでレンダリングし、当日結果を探す。未掲載なら15分後のスケジュールで再試行し、土曜03:00 JSTまで継続する。

- **結果取得後 — ENCODE / ACTUATE**  
  当せん結果を128ビットの固定長フレームへ変換し、秘密鍵が決める8,192チップのローカル計算を実行する。外部サービスへの投稿・改変・大量アクセスは行わない。

## 2. 128ビットフレーム

| 領域 | ビット | 内容 |
|---|---:|---|
| Sync | 16 | `0x829A` |
| Header | 64 | version 4 / 回号 12 / 日付 14 / 本数字組合せ順位 24 / ボーナス順位 9 / reserved 1 |
| Auth | 32 | HMAC-SHA256の先頭32ビット |
| CRC | 16 | CRC-16/CCITT-FALSE |

本数字は `37C7` の辞書順順位、ボーナス数字は本数字を除く30個から2個を選ぶ辞書順順位として格納する。

## 3. 鍵付き拡散

各フレームビットについて64個のチップを作る。HMACから過去ノイズ上の位置、反転PNビット、未来側での実行順を決める。

```text
past_position = HMAC(K, date || bit || sample)[0:8] mod 262144
pn            = HMAC(...)[8] & 1
future_chip   = frame_bit XOR pn
```

受信側は指定位置の最下位ビットとPNからフレームビットを多数決する。送信側は結果確定後、同じチップ列を固定時間・同量計算の二分岐として実行する。

## 4. 受理条件

1. Syncが一致する。
2. CRCが一致する。
3. 秘密鍵によるHMACタグが一致する。
4. フレームの日付が受信日と一致する。
5. 1〜37の本数字7個とボーナス数字2個として妥当である。
6. 受信記録が当該金曜日18:00 JSTより前に作成されている。

近い番号や部分一致は受信として扱わない。

## 5. 実験上の境界

このコードは既知の物理学で未来から過去へ情報を送れることを保証しない。実装するのは、事前固定された受信、公式結果の無人取得、認証符号化、外部書き込みをしない鍵付きローカル計算、事後照合である。
